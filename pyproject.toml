[tool.ruff]
target-version = "py312"
line-length = 120
exclude = [".venv", ".codegraph", "data"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "B"]
ignore = ["B008"]  # FastAPI intentionally declares dependencies in defaults.

[tool.ruff.format]
quote-style = "double"
